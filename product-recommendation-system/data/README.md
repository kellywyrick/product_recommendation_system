# Data

`sample_ratings.csv` is a 5,000-row reproducible sample of the filtered modeling dataset for repository inspection.

The full Amazon Electronics ratings CSV is approximately 318 MB and is intentionally excluded from Git. To rerun the full analysis, place it at `data/ratings_Electronics.csv`. The source file has four headerless columns: `user_id`, `prod_id`, `rating`, `timestamp`.
